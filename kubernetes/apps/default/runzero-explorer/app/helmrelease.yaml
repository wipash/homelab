---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: runzero-explorer
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controller:
      type: statefulset
      annotations:
        reloader.stakater.com/auto: "true"
    image:
      repository: ghcr.io/wipash/runzero
      tag: latest@sha256:31c50fb7955957b3e975e12845e71d7447e87324f50a7bc5ff00de4c4a7e6124
    env:
      TZ: Pacific/Auckland
      RUMBLE_AGENT_LOG_DEBUG: false
    envFrom:
      - secretRef:
          name: runzero-explorer-secret
    service:
      main:
        enabled: false
    securityContext:
      privileged: true
    ingress:
      main:
        enabled: false
    resources:
      requests:
        cpu: 10m
        memory: 100Mi
      limits:
        memory: 4000Mi
