---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nzbget-config-v1
  namespace: media
  annotations:
    nfs.io/storage-path: "nzbget"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-provisioner
