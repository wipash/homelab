---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hi-events-dev
spec:
  chartRef:
    kind: OCIRepository
    name: hi-events-dev
  interval: 1h
  values:
    controllers:
      hi-events-dev:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18
            envFrom: &envFrom
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
        containers:
          app:
            image:
              repository: ghcr.io/wipash/hi.events
              tag: latest@sha256:849117255385f71916046d871350d4676c2a6a4e66667b87d11cb4e355862a16
            env:
              TZ: Pacific/Auckland
              LOG_CHANNEL: stderr
            envFrom: *envFrom
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
                spec:
                  failureThreshold: 30
                  periodSeconds: 5
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 1Gi

          mailpit:
            image:
              repository: ghcr.io/axllent/mailpit
              tag: v1.29.1@sha256:7c38788069433ba9d2d9131abac8d17da0fb18f09b689207ec7a773dcb8d6eee
            env:
              TZ: Pacific/Auckland


    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 82 # www-data, required for serversideup/php base image
        runAsGroup: 82
        fsGroup: 82
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile: { type: RuntimeDefault }
    service:
      app:
        ports:
          http:
            port: 8080
      mailpit:
        ports:
          http:
            port: 8025
    ingress:
      app:
        className: external
        hosts:
          - host: "events-dev.mcgrath.nz"
            paths:
              - path: /
                service:
                  identifier: app
                  port: http
      mailpit:
        className: internal
        hosts:
          - host: "events-dev-mailpit.mcgrath.nz"
            paths:
              - path: /
                service:
                  identifier: mailpit
                  port: http
    persistence:
      storage:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /app/backend/storage

      laravel-cache:
        type: emptyDir
        globalMounts:
          - path: /app/backend/bootstrap/cache

      htmlpurifier-cache:
        type: emptyDir
        globalMounts:
          - path: /app/backend/vendor/ezyang/htmlpurifier/library/HTMLPurifier/DefinitionCache/Serializer

      nginx-data:
        type: emptyDir
        globalMounts:
          - path: /var/lib/nginx

      run:
        type: emptyDir
        globalMounts:
          - path: /var/run

      home-www-data:
        type: emptyDir
        globalMounts:
          - path: /home/www-data

      tmp:
        type: emptyDir
