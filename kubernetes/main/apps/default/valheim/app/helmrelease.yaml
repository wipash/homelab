---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2beta2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app valheim
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.7.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 10m

  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3

  values:
    controllers:
      valheim:
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: lloesche/valheim-server
              tag: latest@sha256:b0a69a47cab987c7583cbaa4eea5bc54a17f2e9c82999c2c8e53454d7fd073b1

            env:
              TZ: Pacific/Auckland
              SERVER_NAME: smellheim
              WORLD_NAME: smellheim
              SERVER_PORT: 2456 #udp
              SERVER_PUBLIC: false
              UPDATE_CRON: "*/60 * * * *"

            envFrom:
              - secretRef:
                  name: valheim-secret
              - configMapRef:
                  name: valheimplus-config

            resources:
              requests:
                memory: 4Gi
                cpu: 200m
              limits:
                memory: 7Gi

    service:
      app:
        controller: *app
        type: LoadBalancer
        annotations:
          io.cilium/lb-ipam-ips: "${SVC_VALHEIM_ADDR:=temp}"
        ports:
          steam1:
            port: 2456
            protocol: UDP
          steam2:
            port: 2457
            protocol: UDP

    persistence:
      config:
        existingClaim: *app
