---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homebox-v1
  namespace: home
  annotations:
    nfs.io/storage-path: "homebox"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-provisioner
