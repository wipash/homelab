---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: your-spotify-server
  namespace: home
spec:
  interval: 10m0s
  chart:
    spec:
      chart: app-template
      version: 1.2.1
      interval: 10m0s
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    image:
      repository: yooooomi/your_spotify_server
      pullPolicy: IfNotPresent
      tag: latest@sha256:b4c410cc0408b1896effb060e11f71e5c71e46fd451ffd54482e75a00c55c1e2

    dependsOn:
      name: your-spotify-mongo
      namespace: home
    env:
      TIMEZONE: ${TIMEZONE}
      API_ENDPOINT: https://spotifyapi.${SECRET_DOMAIN}
      CLIENT_ENDPOINT: &client-url https://spotify.${SECRET_DOMAIN}
      SPOTIFY_PUBLIC:
        secretKeyRef:
          name: your-spotify
          key: client-id
      SPOTIFY_SECRET:
        secretKeyRef:
          name: your-spotify
          key: secret
      MONGO_ENDPOINT: mongodb://your-spotify-mongo.home.svc.cluster.local:27017/your_spotify
      CORS: *client-url

    service:
      main:
        ports:
          http:
            port: 8080

    ingress:
      main:
        enabled: true
        ingressClassName: "nginx"
        annotations:
          external-dns/is-public: "true"
          external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
          external-dns.alpha.kubernetes.io/target: "${SECRET_CLOUDFLARE_TUNNEL_ID}.cfargotunnel.com"
        hosts:
          - host: &your-spotify-server-host "spotifyapi.${SECRET_DOMAIN}"
            paths:
              - path: /
        tls:
          - hosts:
              - *your-spotify-server-host

    podAnnotations:
      secret.reloader.stakater.com/reload: your-spotify
