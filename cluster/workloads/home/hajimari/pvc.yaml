---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-data-v1
  namespace: home
  annotations:
    nfs.io/storage-path: "hajimari"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-provisioner
