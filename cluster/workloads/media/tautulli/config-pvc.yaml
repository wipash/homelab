---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-config-v1
  namespace: media
  annotations:
    nfs.io/storage-path: "tautulli"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-provisioner
