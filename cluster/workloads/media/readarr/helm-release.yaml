---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: readarr
  namespace: media
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.2.1
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    image:
      repository: ghcr.io/onedr0p/readarr-nightly
      tag: 0.1.4.1594@sha256:9dc5d555beed53ea5589e7b0d4e8ccd242ff86114c09d756650b9e4f33eae66b

    podAnnotations:
      secret.reloader.stakater.com/reload: readarr

    podSecurityContext:
      supplementalGroups:
        - 65537 # NAS Permission group

    env:
      PUSHOVER_APP_URL: &host "readarr.${SECRET_DOMAIN}"
      PUSHOVER_PRIORITY: "-1"
      readarr__INSTANCE_NAME: readarr
      readarr__PORT: &port 8787
      readarr__APPLICATION_URL: "https://readarr.${SECRET_DOMAIN}"
      readarr__LOG_LEVEL: info

    envFrom:
      - secretRef:
          name: readarr

    service:
      main:
        ports:
          http:
            port: *port

    ingress:
      main:
        enabled: true
        ingressClassName: "nginx"
        annotations:
          nginx.ingress.kubernetes.io/enable-cors: "true"
          nginx.ingress.kubernetes.io/cors-allow-origin: "https://homer.${SECRET_DOMAIN}"
          nginx.ingress.kubernetes.io/configuration-snippet: |
            proxy_set_header Accept-Encoding "";
            sub_filter '</head>' '<link rel="stylesheet" type="text/css" href="https://theme-park.${SECRET_DOMAIN}/css/base/readarr/nord.css"></head>';
            sub_filter_once on;
          hajimari.io/enable: "true"
          hajimari.io/appName: readarr
        hosts:
          - host: *host
            paths:
              - path: /
        tls:
          - hosts:
              - *host

    persistence:
      config:
        enabled: true
        existingClaim: readarr-config-v1
      media:
        enabled: true
        existingClaim: media-nfs
        mountPath: /data/nas-media
